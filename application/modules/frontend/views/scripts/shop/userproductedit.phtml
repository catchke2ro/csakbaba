<div class="productEditAddForm " data-balance="<?=$this->user->balance ?: 0;?>"
     data-productid="<?=($this->product ? $this->product_id : '');?>"
     data-categoryid="<?=($this->category ? $this->category->id : '');?>"
>

    <p class="infoText selectCategory">
        Válaszd ki, hogy melyik kategóriába szeretnéd feltölteni az eladó termékedet!
    </p>

    <div class="categorySelects <?=($this->product ? 'hasProduct' : '');?>">
        <div class="borzeIcons">
            <?php foreach($this->catMultiArray as $mainCat){?>
                <?php $active = $this->category && $this->category->getMainCatId() == $mainCat->id;?>
                <a href="javascript:void(0)" class="<?=$mainCat->id;?> <?=$active ? 'active' : '';?>" data-id="<?=$mainCat->id;?>"><?=$mainCat->name;?></a>
            <?php } ?>
        </div>

        <?php foreach($this->selects as $key=>$select){ ?>
            <?php $visible = $this->category && $this->category->getMainCatId() == $key;?>
            <div class="categorySelect <?=$key;?> <?=$visible ? 'visible' : '';?>">
                <?=$select;?>
            </div>
        <?php } ?>
    </div>
    
    <div class="formActions">
        <a href="javascript:void(0)" class="productEditPopupButton save">Mentés</a>
    </div>
    
    <div class="form">
        
    </div>



</div>

